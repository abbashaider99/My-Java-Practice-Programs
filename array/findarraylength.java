class findarraylength {
    public static void main(String[] args) {
        int[][] arr = new int[3][4];
        // System.out.println(arr.length); // length variable only find the base size of
        // array, we need to aaply techniques
        // to find all size
        for (int i = 0; i <= arr.length; i++) {
            System.out.println(i);
        }

    }
}